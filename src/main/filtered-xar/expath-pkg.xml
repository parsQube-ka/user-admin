<?xml version="1.0" encoding="UTF-8"?>
<package xmlns="http://expath.org/ns/pkg" name="${xar.name}" abbrev="${project.artifactId}" version="${project.version}" spec="1.0">
    <title>${project.name}</title>
    <dependency package="http://exist-db.org/apps/shared"/>
</package>
